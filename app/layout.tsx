import type { Metadata } from "next";import "./globals.css";
export const metadata: Metadata={title:"CREAIONX PROPERTY — Qualified Property Demand",description:"Pakistan's property-demand network connecting serious buyers, sellers and investors with verified realtors."};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
