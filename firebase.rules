rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The MVP performs business-data reads/writes through authenticated Vercel API routes
    // using Firebase Admin. Keep client Firestore closed by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
