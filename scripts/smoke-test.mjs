const base=(process.env.APP_URL||process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000").replace(/\/$/,"");
const health=await fetch(`${base}/api/health`);const healthBody=await health.json().catch(()=>({}));console.log("HEALTH",health.status,JSON.stringify(healthBody,null,2));if(!health.ok)process.exitCode=1;
const unique=String(Date.now()).slice(-7);const phone=`030${unique}`.slice(0,11);
const payload={type:"buy",city:"Karachi",area:"DHA",propertyType:"House",budgetMin:30000000,budgetMax:50000000,size:"500 sq yd",paymentMode:"Cash",purpose:"Automated smoke test",timeframe:"Within 30 days",name:"Blueprint Smoke Test",phone,email:"",contactConsent:true,source:"smoke-test",utmSource:"production-smoke-test"};
const lead=await fetch(`${base}/api/leads`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const leadBody=await lead.json().catch(()=>({}));console.log("LEAD",lead.status,JSON.stringify(leadBody,null,2));if(!lead.ok){process.exitCode=1}else{const track=await fetch(`${base}/api/track`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({leadId:leadBody.leadId,phone})});const trackBody=await track.json().catch(()=>({}));console.log("TRACK",track.status,JSON.stringify(trackBody,null,2));if(!track.ok)process.exitCode=1;}
